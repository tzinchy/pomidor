# from fastapi import Cookie
# from fastapi import APIRouter, Body, Response

# router = APIRouter('mock_oath', prefix=['mock-oath'])

# @router.post('mock_oath')
# async def mock_oauth(type : int = Body(...), respone = Response):
#     if type == 1: 
#         respone.set_cookie()

